--ozone-platform=wayland
--ozone-platform-hint=wayland
--enable-features=TouchpadOverscrollHistoryNavigation
--load-extension=/usr/share/omarchy/default/chromium/extensions/copy-url,/usr/share/omarchy/default/chromium/extensions/yt-dlp,/usr/share/omarchy/default/chromium/extensions/whatsapp-slim
--password-store=gnome-libsecret

# 2026-09-03: VA-API hardware video decode allocates NV12 dmabuf frames that
# this 3-driver setup (i915 + nvidia + evdi/DisplayLink) cannot import into EGL.
# Symptom: eglCreateImage EGL_BAD_MATCH ~30x/sec, stutter, missing styles.
# Measured: 908 failures/16s -> 0 with this flag.
--disable-accelerated-video-decode
