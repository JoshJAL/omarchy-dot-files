# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Load hyprpm plugins (dynamic-cursors shake-to-enlarge, etc.)
exec-once = hyprpm reload

# Keep system awake while audio is playing or CPU is busy (prevents auto-lock)
exec-once = ~/.local/bin/keepawake-guard
